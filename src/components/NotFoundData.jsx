const NotFoundData = ({ children }) => {
  return (
    <h4 className="text-2xl w-3/4 mx-auto text-center font-semibold text-stone-200 h-[80vh] flex flex-col items-center justify-center">
      {children}
    </h4>
  );
};

export default NotFoundData;
