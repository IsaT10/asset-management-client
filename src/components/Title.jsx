const Title = ({ title }) => {
  return (
    <h4 className="text-lg sm:text-2xl md:text-3xl uppercase mt-16 mb-8 md:mb-16 font-semibold text-stone-200">
      {title}
    </h4>
  );
};

export default Title;
